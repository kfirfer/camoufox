cloverlabs-camoufox
# 1.61 sent viewport.isMobile in Browser.setDefaultViewport, which this
# juggler's protocol schema rejected; the schema now accepts it, so the cap
# moves up rather than away. Matches the cap in pythonlib/pyproject.toml.
playwright<1.63
